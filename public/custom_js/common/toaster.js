// Set Toastr options (optional)
toastr.options = {
    positionClass: 'toast-top-right',
    progressBar: true,
    closeButton: true,
    timeOut: 5000,
    extendedTimeOut: 2000
};

window.toastr = toastr;